{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build and Run Expense Tracker",
            "type": "shell",
            "command": "javac -d bin (Get-ChildItem -Recurse -Filter *.java | ForEach-Object { $_.FullName }); if ($?) { java -cp bin Main }",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": "$javac",
            "detail": "Compiles all Java files and runs Main.java automatically"
        }
    ]
}
