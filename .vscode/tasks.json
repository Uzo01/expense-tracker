{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build Expense Tracker",
      "type": "shell",
      "command": "if (Test-Path bin) { Remove-Item -Recurse -Force bin }; mkdir bin; Get-ChildItem -Recurse -Filter *.java src | ForEach-Object { javac --module-path \"C:\\Libraries\\javafx-sdk-25\\lib\" --add-modules javafx.controls,javafx.fxml -d bin $_.FullName }",
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": []
    },
    {
      "label": "Run Expense Tracker GUI",
      "type": "shell",
      "command": "java --module-path \"C:\\Libraries\\javafx-sdk-25\\lib\" --add-modules javafx.controls,javafx.fxml -cp bin gui.MainGUI",
      "group": {
        "kind": "test",
        "isDefault": true
      },
      "dependsOn": "Build Expense Tracker"
    }
  ]
}
