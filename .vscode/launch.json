{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build and Run Java",
            "type": "shell",
            "command": "javac -d bin src/Main.java && java -cp bin Main",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": "$javac",
            "detail": "Compiles and runs the Expense Tracker project"
        }
    ]
}
